// PlanetSelector.styles.ts
import { SCANNER } from "../theme";

export const PLANETSELECTOR_STYLES = {
  wrapper: "w-full font-mono pointer-events-auto shrink-0",
  box: SCANNER.box,
  header: SCANNER.header,
  headerLabel: SCANNER.headerLabel,
  headerCount: SCANNER.headerCount,
  list: SCANNER.list,
  item: SCANNER.item,
  itemIndex: "text-[10px] text-cyan-600 font-bold",
  emptyState: "text-[11px] text-white/40 p-2 text-center italic",
};
